/**
 * Background JS — disabled for new bold design.
 * Canvas is hidden via CSS (display:none in style.css).
 * This file is kept for compatibility; it does nothing.
 */
const canvas = document.getElementById('bg-canvas');
if (canvas) canvas.style.display = 'none';
